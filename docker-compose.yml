version: '3'

services:
  postgres:
    image: postgres
    restart: unless-stopped
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - app-network
  redis:
    image: redis
    restart: unless-stopped
    networks:
      - app-network
  server:
    depends_on:
      - postgres
    image: duckluckmarket/godeploy:4bc074fd
    container_name: server
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./log:/log
    networks:
      - app-network
networks:
  app-network:
    driver: bridge